/* 
 * Problem: 
 * Meisi Li
 * Date: 
 */

#include <iostream>
#include <cstdio>
#include <cmath>
#include <cstring>
#include <algorithm>
#include <string>
#include <vector>
#include <stack>
#include <queue>
#include <set>
#include <map>
#include <sstream>
#include <complex>
#include <ctime>
#include <cassert>
#include <functional>
#include <list>
using namespace std;

#define IEF(i, s, t)    for(i=(s); i<(t); i++)
#define NEF(i, s, t)    for(i=(s); i!=(t); i++)
#define MAXN 100000
#define INF (int)1e9

int main(){
    ios_base::sync_with_stdio(false);
    
    return 0;
}
